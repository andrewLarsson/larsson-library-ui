{
	"name": "larsson-library-ui",
	"version": "0.1.0",
	"description": "A UI for the Larsson Library API.",
	"repository": "https://github.com/andrewLarsson/larsson-library-ui",
	"devDependencies": {
		"bower": "^1.3.1",
		"grunt": "^0.4.5",
		"http-server": "^0.6.1",
		"shelljs": "^0.2.6"
	},
	"scripts": {
		"postinstall": "bower install",
		"prestart": "npm install",
		"start": "http-server -a localhost -p 8000 -c-1",
		"preupdate-webdriver": "npm install",
		"update-webdriver": "webdriver-manager update"
	}
}
